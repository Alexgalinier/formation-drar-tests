const { multiplyBy2AndFilterEvens } = require('./number-helper');

describe('Number Helper', () => {
  describe('multiplyBy2AndFilterEvens', () => {
    it('Only works with numbers, if not, an Error occured', () => {
      // Ecrire le test
      // Il y a peut être des infos intéressantes ici : https://jestjs.io/docs/en/expect#tothrowerror
    });
  });
});
