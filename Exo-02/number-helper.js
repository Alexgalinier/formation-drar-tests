module.exports = {
  // La fonction va prendre un tableau de nombre (entier ou flotant) en entrée,
  // multiplier tous les nombres par 2 puis filtrer ceux qui ne sont pas des nombres pairs.
  // Le résultat est renvoyé en sortie.
  //
  // TDD style! Ecrire le test en premier, puis la fonction
  multiplyBy2AndFilterEvens: numbers => {
    return [];
  },
};
